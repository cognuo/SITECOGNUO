---
interface Props {
  title?: string;
  description?: string;
  canonical?: string;
}

const {
  title = "Cognuo | Software sob medida e automações com IA",
  description = "Desenvolvemos sistemas sob medida, automações inteligentes e integrações que eliminam processos manuais e reduzem custos operacionais.",
  canonical = "https://cognuo.com.br",
} = Astro.props;

const organizationSchema = {
  "@context": "https://schema.org",
  "@type": "Organization",
  name: "Cognuo",
  url: "https://cognuo.com.br",
  logo: "https://cognuo.com.br/images/logo_white.png",
  description:
    "Software house especializada em sistemas sob medida, automações com IA e integrações.",
  address: { "@type": "PostalAddress", addressCountry: "BR" },
  contactPoint: {
    "@type": "ContactPoint",
    contactType: "customer service",
    availableLanguage: "Portuguese",
  },
};

const serviceSchema = {
  "@context": "https://schema.org",
  "@type": "Service",
  name: "Software sob medida e automações com IA",
  provider: { "@type": "Organization", name: "Cognuo" },
  description:
    "Desenvolvimento de software sob medida, automações com IA, agentes inteligentes e integrações de sistemas.",
  areaServed: "BR",
};
---

<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />

    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:locale" content="pt_BR" />
    <meta property="og:site_name" content="Cognuo" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Red+Hat+Display:ital,wght@0,400;0,500;0,600;0,700;0,800;0,900;1,400;1,500&display=swap"
      rel="stylesheet"
    />
    <link rel="icon" type="image/png" href="/images/favicon.png" />

    <!-- Meta Pixel -->
    <script is:inline>
      !function(f,b,e,v,n,t,s){if(f.fbq)return;n=f.fbq=function(){n.callMethod?
      n.callMethod.apply(n,arguments):n.queue.push(arguments)};if(!f._fbq)f._fbq=n;
      n.push=n;n.loaded=!0;n.version='2.0';n.queue=[];t=b.createElement(e);t.async=!0;
      t.src=v;s=b.getElementsByTagName(e)[0];s.parentNode.insertBefore(t,s)}(window,
      document,'script','https://connect.facebook.net/en_US/fbevents.js');
      fbq('init','1340251530928356');
      fbq('track','PageView');
    </script>
    <noscript>
      <img height="1" width="1" style="display:none"
        src="https://www.facebook.com/tr?id=1340251530928356&ev=PageView&noscript=1"/>
    </noscript>
    <!-- End Meta Pixel -->

    <script
      type="application/ld+json"
      set:html={JSON.stringify(organizationSchema)}
    />
    <script
      type="application/ld+json"
      set:html={JSON.stringify(serviceSchema)}
    />
  </head>
  <body class="bg-off-white font-sans antialiased text-secondary">
    <slot />

    <!-- Floating WhatsApp button (all pages) -->
    <a
      class="wa-float"
      href="https://wa.me/5511991203467?text=Ol%C3%A1!%20Vim%20pelo%20site%20da%20Cognuo%20e%20gostaria%20de%20saber%20mais."
      target="_blank"
      rel="noopener noreferrer"
      aria-label="Falar no WhatsApp"
    >
      <span class="wa-float-ic" aria-hidden="true">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
          <path d="M.057 24l1.687-6.163a11.867 11.867 0 01-1.587-5.945C.16 5.335 5.495 0 12.05 0a11.82 11.82 0 018.413 3.488 11.82 11.82 0 013.48 8.414c-.003 6.557-5.338 11.892-11.893 11.892a11.9 11.9 0 01-5.688-1.448L.057 24zm6.597-3.807c1.676.995 3.276 1.591 5.392 1.592 5.448 0 9.886-4.434 9.889-9.885.002-5.462-4.415-9.89-9.881-9.892-5.452 0-9.887 4.434-9.889 9.884a9.86 9.86 0 001.523 5.26l-.999 3.648 3.965-1.039zm11.387-5.464c-.074-.124-.272-.198-.57-.347-.297-.149-1.758-.868-2.031-.967-.272-.099-.47-.149-.669.149-.198.297-.768.967-.941 1.165-.173.198-.347.223-.644.074-.297-.149-1.255-.462-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.297-.347.446-.521.151-.172.2-.296.3-.495.099-.198.05-.372-.025-.521-.075-.148-.669-1.611-.916-2.206-.242-.579-.487-.5-.669-.51l-.57-.01c-.198 0-.52.074-.792.372s-1.04 1.016-1.04 2.479 1.065 2.876 1.213 3.074c.149.198 2.095 3.2 5.076 4.487.709.306 1.263.489 1.694.626.712.226 1.36.194 1.872.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413z"/>
        </svg>
      </span>
      <span class="wa-float-label">Fale no WhatsApp</span>
    </a>
  </body>
</html>

<style is:global>
  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  @font-face {
    font-family: "Instrument Sans Variable";
    font-style: normal;
    font-weight: 400 700;
    font-display: swap;
    src: url("/fonts/instrument-sans-variable.woff2") format("woff2-variations");
  }

  /* Sized to match Instrument Sans metrics, prevents CLS during font swap */
  @font-face {
    font-family: "Instrument Sans Fallback";
    src: local("Arial");
    size-adjust: 99%;
    ascent-override: 95%;
    descent-override: 24%;
    line-gap-override: 0%;
  }

  body {
    font-family:
      "Red Hat Display", "Instrument Sans Fallback", ui-sans-serif,
      system-ui, sans-serif;
    -webkit-font-smoothing: antialiased;
    text-rendering: optimizeSpeed;
  }

  /* Distrito-style highlight on key words inside headlines (Cognuo blue).
     .hl      → solid blue, for use on LIGHT backgrounds.
     .hl-grad → bright blue gradient, for use on DARK backgrounds. */
  .hl {
    color: #1f4fd0;
  }
  .hl-grad {
    background: linear-gradient(100deg, #5c8df5 0%, #84acff 55%, #b9d0ff 100%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  /* ───── Floating WhatsApp button (global) ─────
     A refined dark-glass pill — not the cliché green bubble. Green is confined
     to a small icon badge; the pill expands on hover to reveal the label. */
  .wa-float {
    position: fixed;
    right: clamp(16px, 3vw, 28px);
    bottom: clamp(16px, 3vw, 28px);
    z-index: 50;
    display: inline-flex;
    align-items: center;
    height: 56px;
    padding: 7px;
    border-radius: 999px;
    background: rgba(16, 16, 16, 0.66);
    border: 1px solid rgba(255, 255, 255, 0.12);
    -webkit-backdrop-filter: blur(14px) saturate(160%);
    backdrop-filter: blur(14px) saturate(160%);
    color: #fff;
    text-decoration: none;
    overflow: hidden;
    max-width: 56px;
    box-shadow:
      0 14px 34px -10px rgba(0, 0, 0, 0.5),
      inset 0 1px 0 rgba(255, 255, 255, 0.08);
    transition:
      max-width 0.62s cubic-bezier(0.65, 0, 0.2, 1),
      box-shadow 0.35s ease,
      border-color 0.35s ease,
      transform 0.25s ease;
    will-change: max-width;
  }
  .wa-float-ic {
    position: relative;
    flex: 0 0 42px;
    width: 42px;
    height: 42px;
    border-radius: 50%;
    display: grid;
    place-items: center;
    color: #fff;
    background: linear-gradient(150deg, #2ce06d 0%, #18b257 60%, #109b4c 100%);
    box-shadow:
      0 4px 12px -2px rgba(37, 211, 102, 0.55),
      inset 0 1px 0 rgba(255, 255, 255, 0.25);
  }
  .wa-float-label {
    font-size: 13.5px;
    font-weight: 600;
    letter-spacing: -0.01em;
    white-space: nowrap;
    padding: 0 16px 0 11px;
    opacity: 0;
    transform: translateX(-6px);
    transition:
      opacity 0.3s ease 0.18s,
      transform 0.5s cubic-bezier(0.65, 0, 0.2, 1) 0.14s;
  }
  .wa-float:hover,
  .wa-float:focus-visible {
    max-width: 234px;
    border-color: rgba(44, 224, 109, 0.5);
    box-shadow:
      0 18px 40px -10px rgba(0, 0, 0, 0.55),
      0 0 0 1px rgba(44, 224, 109, 0.25),
      inset 0 1px 0 rgba(255, 255, 255, 0.08);
  }
  .wa-float:hover .wa-float-label,
  .wa-float:focus-visible .wa-float-label {
    opacity: 1;
    transform: translateX(0);
  }
  .wa-float:active {
    transform: scale(0.97);
  }
  @media (prefers-reduced-motion: reduce) {
    .wa-float {
      transition: box-shadow 0.2s ease;
    }
    .wa-float-label {
      transition: opacity 0.2s ease;
      transform: none;
    }
  }

  [data-animate] {
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 0.65s ease,
      transform 0.65s ease;
  }
  [data-animate].visible {
    opacity: 1;
    transform: translateY(0);
  }
  [data-animate-delay="1"] {
    transition-delay: 0.1s;
  }
  [data-animate-delay="2"] {
    transition-delay: 0.2s;
  }
  [data-animate-delay="3"] {
    transition-delay: 0.3s;
  }
  [data-animate-delay="4"] {
    transition-delay: 0.4s;
  }

  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(18px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  .anim-hero {
    opacity: 0;
    animation: fadeUp 0.8s ease forwards;
  }
  .anim-d1 {
    animation-delay: 0.05s;
  }
  .anim-d2 {
    animation-delay: 0.2s;
  }
  .anim-d3 {
    animation-delay: 0.35s;
  }
  .anim-d4 {
    animation-delay: 0.5s;
  }

  /* ───── HERO ───── */
  .hero-root {
    isolation: isolate;
  }

  .hero-grid-cols {
    background-image: linear-gradient(
      to right,
      rgba(250, 250, 250, 0.038) 1px,
      transparent 1px
    );
    background-size: 8.3333% 100%;
    -webkit-mask-image: linear-gradient(
      to bottom,
      transparent 0%,
      black 22%,
      black 78%,
      transparent 100%
    );
    mask-image: linear-gradient(
      to bottom,
      transparent 0%,
      black 22%,
      black 78%,
      transparent 100%
    );
  }

  .hero-grid-dots {
    background-image: radial-gradient(
      circle at 1px 1px,
      rgba(250, 250, 250, 0.06) 1px,
      transparent 0
    );
    background-size: 32px 32px;
  }

  .hero-orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(60px);
    pointer-events: none;
    will-change: opacity, transform;
  }

  .hero-orb-1 {
    right: -12%;
    bottom: -18%;
    width: min(820px, 85vw);
    height: min(820px, 85vw);
    background: radial-gradient(
      circle,
      rgba(0, 53, 176, 0.34) 0%,
      rgba(0, 53, 176, 0.08) 40%,
      transparent 65%
    );
    animation: orbBreathe 10s ease-in-out infinite;
  }

  .hero-orb-2 {
    left: -8%;
    top: 8%;
    width: min(520px, 65vw);
    height: min(520px, 65vw);
    background: radial-gradient(
      circle,
      rgba(0, 53, 176, 0.14) 0%,
      transparent 60%
    );
    animation: orbBreathe 13s ease-in-out infinite reverse;
  }

  @keyframes orbBreathe {
    0%,
    100% {
      opacity: 0.95;
      transform: scale(1);
    }
    50% {
      opacity: 0.65;
      transform: scale(1.09);
    }
  }

  .hero-spotlight {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: radial-gradient(
      540px circle at var(--mx, 30%) var(--my, 42%),
      rgba(0, 53, 176, 0.14),
      transparent 45%
    );
    transition: background 0.2s ease-out;
    mix-blend-mode: screen;
  }

  .hero-accent-line {
    position: absolute;
    top: 72px;
    left: 0;
    right: 0;
    height: 1px;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(0, 53, 176, 0) 16%,
      rgba(0, 53, 176, 0.42) 50%,
      rgba(0, 53, 176, 0) 84%,
      transparent 100%
    );
    pointer-events: none;
  }

  .hero-cta-primary {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.95rem 1.75rem;
    background: #0035b0;
    color: #ffffff;
    font-size: 14px;
    font-weight: 700;
    letter-spacing: -0.01em;
    border-radius: 999px;
    transition:
      background-color 0.25s ease,
      box-shadow 0.35s ease,
      transform 0.25s ease;
    box-shadow:
      inset 0 1px 0 0 rgba(255, 255, 255, 0.18),
      0 14px 30px -10px rgba(0, 53, 176, 0.5);
  }
  .hero-cta-primary:hover {
    background: #0040d4;
    transform: translateY(-1px);
    box-shadow:
      inset 0 1px 0 0 rgba(255, 255, 255, 0.22),
      0 22px 50px -10px rgba(0, 53, 176, 0.65);
  }
  .hero-cta-primary svg {
    transition: transform 0.3s ease;
  }
  .hero-cta-primary:hover svg {
    transform: translateX(3px);
  }

  .hero-cta-secondary {
    display: inline-flex;
    align-items: center;
    padding: 0.95rem 1.6rem;
    color: rgba(250, 250, 250, 0.82);
    font-size: 14px;
    font-weight: 600;
    border: 1px solid rgba(250, 250, 250, 0.3);
    border-radius: 999px;
    transition:
      color 0.2s ease,
      border-color 0.2s ease,
      background-color 0.2s ease;
  }
  .hero-cta-secondary:hover {
    color: rgba(250, 250, 250, 1);
    border-color: rgba(250, 250, 250, 0.4);
    background: rgba(250, 250, 250, 0.03);
  }

  .hero-scroll-chev {
    animation: scrollBounce 2s ease-in-out infinite;
  }
  @keyframes scrollBounce {
    0%,
    100% {
      transform: translateY(0);
      opacity: 0.7;
    }
    50% {
      transform: translateY(3px);
      opacity: 1;
    }
  }

  #navbar.scrolled {
    background: rgba(16, 16, 16, 0.96);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(250, 250, 250, 0.06);
  }

  details > summary {
    list-style: none;
  }
  details > summary::-webkit-details-marker {
    display: none;
  }
  details[open] .faq-plus {
    transform: rotate(45deg);
  }
  .faq-plus {
    transition: transform 0.2s ease;
    display: inline-block;
  }

  /* ───── Hero figure: orbit system ───── */
  .hero-figure {
    position: relative;
    width: 100%;
    aspect-ratio: 1;
    max-width: 560px;
    margin-left: auto;
  }
  .hero-figure .hf-wires {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }
  .hf-core {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
    width: 230px;
    padding: 1.4rem 1.2rem;
    text-align: center;
    background: rgba(0, 53, 176, 0.14);
    border: 1px solid rgba(0, 53, 176, 0.45);
    border-radius: 18px;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    box-shadow:
      0 30px 80px -22px rgba(0, 53, 176, 0.6),
      inset 0 1px 0 rgba(255, 255, 255, 0.1);
  }
  .hf-core-label {
    font-size: 15px;
    font-weight: 600;
    color: #fafafa;
    letter-spacing: -0.01em;
  }
  .hf-core-sub {
    margin-top: 6px;
    font-size: 10.5px;
    font-weight: 500;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: rgba(250, 250, 250, 0.6);
  }
  .hf-pulse {
    position: absolute;
    inset: -6px;
    border: 1px solid rgba(0, 53, 176, 0.45);
    border-radius: 22px;
    animation: hfPulse 2.6s ease-out infinite;
    pointer-events: none;
  }
  @keyframes hfPulse {
    0% {
      opacity: 0.8;
      transform: scale(1);
    }
    100% {
      opacity: 0;
      transform: scale(1.22);
    }
  }
  .hf-node {
    position: absolute;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.55rem 0.85rem;
    font-size: 12.5px;
    font-weight: 500;
    color: rgba(250, 250, 250, 0.9);
    background: rgba(16, 16, 16, 0.88);
    border: 1px solid rgba(250, 250, 250, 0.16);
    border-radius: 999px;
    backdrop-filter: blur(6px);
    animation: hfFloat 5s ease-in-out infinite;
  }
  .hf-node svg {
    width: 16px;
    height: 16px;
    color: #6b8ef0;
  }
  .hf-node-a {
    top: 18%;
    left: 8%;
    animation-delay: 0s;
  }
  .hf-node-b {
    top: 18%;
    right: 8%;
    animation-delay: 0.6s;
  }
  .hf-node-c {
    bottom: 18%;
    left: 8%;
    animation-delay: 1.2s;
  }
  .hf-node-d {
    bottom: 18%;
    right: 8%;
    animation-delay: 1.8s;
  }
  .hf-node-e {
    top: 3%;
    left: 50%;
    transform: translateX(-50%);
    animation-delay: 2.4s;
  }
  @keyframes hfFloat {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-5px);
    }
  }
  .hf-node-e {
    animation-name: hfFloatCenter;
  }
  @keyframes hfFloatCenter {
    0%,
    100% {
      transform: translate(-50%, 0);
    }
    50% {
      transform: translate(-50%, -5px);
    }
  }

  /* ───── Before/After positioning figure ───── */
  .pos-card {
    position: relative;
    padding: 1.4rem 1.2rem;
    min-height: 320px;
    border-radius: 20px;
    display: flex;
    flex-direction: column;
  }
  .pos-before {
    background: #eef1f8;
    border: 1px solid rgba(16, 16, 16, 0.08);
    color: rgba(16, 16, 16, 0.45);
  }
  .pos-after {
    background: #101010;
    color: rgba(250, 250, 250, 0.85);
  }
  .pos-label {
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    opacity: 0.7;
    margin-bottom: 1rem;
  }
  .pos-chaos,
  .pos-unified {
    position: relative;
    flex: 1;
    min-height: 220px;
  }
  .pos-chip {
    position: absolute;
    padding: 0.28rem 0.6rem;
    font-size: 10.5px;
    font-weight: 500;
    background: rgba(16, 16, 16, 0.06);
    color: rgba(16, 16, 16, 0.7);
    border: 1px solid rgba(16, 16, 16, 0.08);
    border-radius: 999px;
    white-space: nowrap;
  }
  .pos-lines {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    color: rgba(16, 16, 16, 0.22);
    pointer-events: none;
  }
  .pos-hub {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    padding: 0.6rem 1rem;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: -0.005em;
    color: #fafafa;
    background: #0035b0;
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 999px;
    white-space: nowrap;
    z-index: 2;
    box-shadow: 0 10px 30px -8px rgba(0, 53, 176, 0.7);
  }
  .pos-sat {
    position: absolute;
    padding: 0.25rem 0.55rem;
    font-size: 10px;
    font-weight: 500;
    color: rgba(250, 250, 250, 0.8);
    background: rgba(250, 250, 250, 0.06);
    border: 1px solid rgba(250, 250, 250, 0.14);
    border-radius: 999px;
  }
  .pos-sat-1 {
    top: 14%;
    left: 50%;
    transform: translateX(-50%);
  }
  .pos-sat-2 {
    top: 50%;
    right: 6%;
    transform: translateY(-50%);
  }
  .pos-sat-3 {
    bottom: 14%;
    left: 50%;
    transform: translateX(-50%);
  }
  .pos-sat-4 {
    top: 50%;
    left: 6%;
    transform: translateY(-50%);
  }
  .pos-rings {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    color: rgba(250, 250, 250, 0.65);
    pointer-events: none;
  }
  .pos-caption {
    margin-top: 1rem;
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    opacity: 0.7;
  }

  /* ───── Problem cards ───── */
  .problem-card {
    position: relative;
    padding: 1.6rem 1.4rem 1.5rem;
    background: #ffffff;
    border: 1px solid rgba(16, 16, 16, 0.08);
    border-radius: 16px;
    transition:
      border-color 0.25s ease,
      transform 0.25s ease,
      box-shadow 0.25s ease;
  }
  .problem-card:hover {
    border-color: rgba(0, 53, 176, 0.35);
    transform: translateY(-2px);
    box-shadow: 0 18px 36px -18px rgba(0, 53, 176, 0.35);
  }
  .problem-icon {
    width: 44px;
    height: 44px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 53, 176, 0.08);
    color: #0035b0;
    border-radius: 12px;
    margin-bottom: 1.1rem;
    transition:
      background 0.25s ease,
      color 0.25s ease;
  }
  .problem-card:hover .problem-icon {
    background: #0035b0;
    color: #fafafa;
  }

  /* ───── Solution cards ───── */
  .sol-card {
    position: relative;
    padding: 1.7rem 1.5rem 1.5rem;
    background: #ffffff;
    border: 1px solid rgba(16, 16, 16, 0.08);
    border-radius: 18px;
    display: flex;
    flex-direction: column;
    transition:
      border-color 0.3s ease,
      transform 0.3s ease,
      box-shadow 0.3s ease;
  }
  .sol-card:hover {
    border-color: rgba(0, 53, 176, 0.35);
    transform: translateY(-2px);
    box-shadow: 0 22px 48px -22px rgba(0, 53, 176, 0.35);
  }
  .sol-icon {
    width: 52px;
    height: 52px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #0035b0;
    background: rgba(0, 53, 176, 0.08);
    border-radius: 14px;
    margin-bottom: 1.3rem;
  }
  .sol-flow {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.7rem 0.9rem;
    background: rgba(0, 53, 176, 0.04);
    border: 1px dashed rgba(0, 53, 176, 0.2);
    border-radius: 12px;
    margin-top: auto;
    margin-bottom: 0.9rem;
  }
  .sol-before {
    font-size: 10.5px;
    color: rgba(16, 16, 16, 0.55);
    text-decoration: line-through;
    text-decoration-color: rgba(16, 16, 16, 0.35);
    flex: 1;
  }
  .sol-arrow {
    color: #0035b0;
    flex-shrink: 0;
  }
  .sol-after {
    font-size: 10.5px;
    font-weight: 600;
    color: #0035b0;
    flex: 1;
    text-align: right;
  }
  .sol-tag {
    display: inline-block;
    padding: 0.35rem 0.75rem;
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: #0035b0;
    background: rgba(0, 53, 176, 0.08);
    border-radius: 999px;
    align-self: flex-start;
  }

  /* ───── Process timeline bar ───── */
  .timeline-figure {
    padding: 1.5rem 0.5rem 0.5rem;
  }
  .tl-bar {
    position: relative;
    height: 3px;
    background: rgba(250, 250, 250, 0.1);
    margin: 0 1rem 2.25rem;
  }
  .tl-fill {
    position: absolute;
    inset-block: 0;
    left: 0;
    width: 88%;
    background: linear-gradient(90deg, #0035b0 0%, #3b6ce0 100%);
    box-shadow: 0 0 12px rgba(0, 53, 176, 0.6);
  }
  .tl-marker {
    position: absolute;
    top: -6px;
    transform: translateX(-50%);
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(250, 250, 250, 0.75);
    padding-top: 18px;
    white-space: nowrap;
  }
  .tl-marker::before {
    content: "";
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: #fafafa;
    border: 2px solid #0035b0;
  }
  .tl-marker-end {
    color: #fafafa;
  }
  .tl-marker-end::before {
    background: #0035b0;
    border-color: #fafafa;
    box-shadow: 0 0 0 4px rgba(0, 53, 176, 0.25);
  }
  .tl-legend {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.75rem;
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: rgba(250, 250, 250, 0.55);
  }

  /* ───── Differentials (them vs us) ───── */
  .diff-row {
    padding: 1.1rem 0 1.25rem;
    border-bottom: 1px solid rgba(16, 16, 16, 0.08);
  }
  .diff-title {
    font-size: 0.98rem;
    font-weight: 600;
    color: #101010;
    margin-bottom: 0.75rem;
  }
  .diff-compare {
    display: grid;
    grid-template-columns: 1fr;
    gap: 0.45rem;
  }
  .diff-them,
  .diff-us {
    display: flex;
    align-items: flex-start;
    gap: 0.65rem;
  }
  .diff-pill {
    flex-shrink: 0;
    font-size: 9.5px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    padding: 0.22rem 0.55rem;
    background: rgba(16, 16, 16, 0.06);
    color: rgba(16, 16, 16, 0.5);
    border-radius: 999px;
    margin-top: 1px;
  }
  .diff-pill-us {
    background: rgba(0, 53, 176, 0.1);
    color: #0035b0;
  }
  .diff-text {
    font-size: 0.88rem;
    color: rgba(16, 16, 16, 0.55);
    text-decoration: line-through;
    text-decoration-color: rgba(16, 16, 16, 0.3);
    line-height: 1.5;
  }
  .diff-text-us {
    color: #101010;
    text-decoration: none;
    font-weight: 500;
  }

  /* ───── Case bars ───── */
  .case-bars {
    display: flex;
    flex-direction: column;
    gap: 1.4rem;
  }
  .case-bar {
  }
  .case-bar-top {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 0.45rem;
  }
  .case-bar-label {
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.16em;
    font-weight: 500;
    color: rgba(16, 16, 16, 0.55);
  }
  .case-bar-value {
    font-size: 0.95rem;
    font-weight: 600;
    color: #101010;
    font-feature-settings: "tnum" on;
  }
  .case-bar-track {
    height: 6px;
    background: rgba(16, 16, 16, 0.07);
    border-radius: 999px;
    overflow: hidden;
  }
  .case-bar-fill {
    height: 100%;
    background: #101010;
    border-radius: 999px;
    transform-origin: left;
    animation: barGrow 1.1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
    transform: scaleX(0);
  }
  .case-bar-hl .case-bar-value {
    color: #0035b0;
    font-size: 1.1rem;
  }
  .case-bar-hl .case-bar-fill {
    background: #0035b0;
  }
  @keyframes barGrow {
    to {
      transform: scaleX(1);
    }
  }

  /* ───── Meeting card (mid CTA) ───── */
  .meeting-card {
    background: rgba(250, 250, 250, 0.04);
    border: 1px solid rgba(250, 250, 250, 0.12);
    border-radius: 20px;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    overflow: hidden;
    box-shadow: 0 30px 60px -20px rgba(0, 53, 176, 0.5);
  }
  .mc-header {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.7rem 1rem;
    background: rgba(0, 0, 0, 0.25);
    border-bottom: 1px solid rgba(250, 250, 250, 0.08);
  }
  .mc-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: rgba(250, 250, 250, 0.2);
  }
  .mc-title {
    margin-left: 0.6rem;
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(250, 250, 250, 0.7);
  }
  .mc-body {
    padding: 1.4rem 1.3rem;
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
  }
  .mc-row {
    display: flex;
    align-items: flex-start;
    gap: 0.7rem;
    font-size: 0.9rem;
    color: rgba(250, 250, 250, 0.8);
    line-height: 1.5;
  }
  .mc-check {
    flex-shrink: 0;
    width: 18px;
    height: 18px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: rgba(0, 53, 176, 0.2);
    color: #6b8ef0;
    border-radius: 50%;
    font-size: 11px;
    font-weight: 700;
    margin-top: 1px;
  }
  .mc-row-emph {
    margin-top: 0.4rem;
    padding-top: 0.85rem;
    border-top: 1px solid rgba(250, 250, 250, 0.08);
    color: #fafafa;
    font-weight: 500;
  }
  .mc-row-emph .mc-check {
    background: #0035b0;
    color: #fafafa;
  }
</style>

<script is:inline>
  (function () {
    function getCookie(name) {
      var m = document.cookie.match('(^|;) ?' + name + '=([^;]*)(;|$)');
      return m ? m[2] : null;
    }
    function genId() {
      return 'ev_' + Date.now() + '_' + Math.random().toString(36).slice(2, 9);
    }
    function sendCapi(payload) {
      fetch('/api/meta-event', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload),
      }).catch(function () {});
    }
    document.addEventListener('click', function (e) {
      var el = e.target.closest('a');
      if (!el) return;
      var href = el.getAttribute('href') || '';
      var isLead = href.startsWith('/contato') || href.startsWith('mailto:') || href.indexOf('wa.me') !== -1;
      if (!isLead) return;
      var eventId = genId();
      if (typeof fbq === 'function') fbq('track', 'Lead', {}, { eventID: eventId });
      sendCapi({
        event_name: 'Lead',
        event_id: eventId,
        event_source_url: window.location.href,
        client_user_agent: navigator.userAgent,
        fbc: getCookie('_fbc'),
        fbp: getCookie('_fbp'),
      });
    });
  })();
</script>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("visible");
          observer.unobserve(entry.target);
        }
      });
    },
    { threshold: 0.08, rootMargin: "0px 0px -40px 0px" },
  );
  document
    .querySelectorAll("[data-animate]")
    .forEach((el) => observer.observe(el));

  const navbar = document.getElementById("navbar");
  window.addEventListener(
    "scroll",
    () => {
      if (window.scrollY > 30) navbar?.classList.add("scrolled");
      else navbar?.classList.remove("scrolled");
    },
    { passive: true },
  );

  const hero = document.querySelector(".hero-root");
  if (hero && window.matchMedia("(pointer: fine)").matches) {
    let rafId = 0;
    hero.addEventListener(
      "mousemove",
      (e) => {
        if (rafId) return;
        rafId = requestAnimationFrame(() => {
          const rect = (hero as HTMLElement).getBoundingClientRect();
          const x = ((e.clientX - rect.left) / rect.width) * 100;
          const y = ((e.clientY - rect.top) / rect.height) * 100;
          (hero as HTMLElement).style.setProperty("--mx", `${x}%`);
          (hero as HTMLElement).style.setProperty("--my", `${y}%`);
          rafId = 0;
        });
      },
      { passive: true },
    );
  }
</script>
